import React, {Fragment} from 'react';

const Warning = () => {
    return (
        <Fragment>
            <p>You won! Click on create a new game to repeat</p>
        </Fragment>
    );
};

export default Warning;